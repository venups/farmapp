[project]
name = "travel-planner-backend"
version = "0.1.0"
description = "Travel Planner API backend"
requires-python = ">=3.9"

[tool.pytest.ini_options]
asyncio_mode = "auto"
testpaths = ["tests"]
