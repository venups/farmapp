# MongoDB connection settings
MONGO_URI=mongodb://localhost:27017/travel_planner
# Fallback to mongomock if local MongoDB is unavailable
USE_MONGOMOCK=True

# Application settings
BACKEND_PORT=8000
FRONTEND_PORT=5173

# Debug mode
DEBUG=True